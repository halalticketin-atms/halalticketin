// Temporary check-in access stays in the codebase, but the product flow is
// disabled until the frontend shell supports it reliably.
export const TEMP_CHECK_IN_ACCESS_ENABLED = false;

export const TEMP_CHECK_IN_ACCESS_DISABLED_MESSAGE =
  'Temporary check-in access is currently unavailable while we finish improving the check-in flow.';
